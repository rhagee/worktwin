---
name: worktwin-help
description: List every worktwin command installed on this machine, with the arguments each one takes and a short description. Generated by reading the installed SKILL.md files so the list always matches reality. Cheap, deterministic, safe to spam.
disable-model-invocation: true
allowed-tools: Bash(bash *), Bash(test *)
---

Show the output below verbatim, every single line of it. Do not paraphrase, do not summarise, do not omit lines. After the output, print nothing further.

!`bash "$HOME/.claude/skills/worktwin/bin/worktwin-help"`
